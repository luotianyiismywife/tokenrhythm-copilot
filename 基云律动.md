# 基云律动平台信息

> 平台官方名称：**基元律动**（Token Rhythm / TokenRhythm）
> 运营主体：**北京基元律动科技有限公司**
> 本文档整理自官网（<https://tokenrhythm.studio>）实测抓取，最后更新：2026-08-04

---

## 1. 平台概览

**基元律动**是一个**多模型聚合接入服务**平台（"基云律动"为口语/代称，官方备案名为"基元律动"）。核心卖点：

- **一个 API Key 覆盖主流开源模型**（GLM、DeepSeek、Kimi、Qwen、MiniMax、MiMo 等系列）
- 支持 **OpenAI**、**Anthropic（Claude）** 两种主流协议，统一接入
- 自研 **OpenSquilla 智能路由**：Agentic Routing，语义路由 + 多模型融合，主打"Agent Token 性价比"
- 提供用量统计、模型发现、评测体系等配套能力
- 官网 ICP 备案：京ICP备2026039474号-2

### 核心页面

| 页面 | 地址 | 说明 |
|------|------|------|
| 首页 | <https://tokenrhythm.studio/> | 平台介绍、调用示例 |
| 模型列表 | <https://tokenrhythm.studio/models> | 模型发现、标签筛选、能力识别 |
| API 文档 | <https://tokenrhythm.studio/docs/api-integration> | OpenAI / Anthropic / Embeddings 接入说明 |
| 快速开始 | <https://tokenrhythm.studio/docs/overview> | 注册、保存 Key、首次调用 |
| OpenSquilla | <https://tokenrhythm.studio/docs/opensquilla> | 本地框架接入 |
| FAQ | <https://tokenrhythm.studio/docs/faq> | 账号、Key、用量常见问题 |
| 用户中心 | <https://tokenrhythm.studio/account> | Token 用量（登录后） |

### 用户中心菜单

| 菜单 | 路由 | 内容 |
|------|------|------|
| Token 用量 | `/account` | 所有会话 Token 成本、各模型花费 |
| 邀请有奖 | `/account/referrals` | 邀请好友返额度（邀请注册+下载 OpenSquilla 立享 68 元） |
| 我的账户 | `/account/account` | 余额、额度明细、账号信息 |
| API Key | `/account/keys` | Key 创建与管理 |
| 调用日志 | `/account/logs` | 请求日志与导出 |
| 对话记录 | `/account/conversations` | 历史对话 |
| 操作日志 | `/account/audit-logs` | 安全审计 |
| 个人信息 | `/account/profile` | 个人资料 |

---

## 2. 账号与额度现状（实测）

> 以下为登录后「我的账户」页面实测数据，随使用实时变动。

| 项目 | 数值 |
|------|------|
| 账号状态 | 正常（普通用户） |
| **账户余额** | **¥53.52**（CNY） |
| 可用额度 | ¥53.52 |
| 冻结额度 | ¥0（待结算占用） |
| 活跃 Key | 2 / 10 |
| 累计调用 | 691 次（成功 651 / 错误 38） |
| 累计成本 | ¥14.50 |
| 限时额度 | 新用户赠送 ¥68，剩余 ¥53.52，**到期 2026-09-03 11:20:12** |

> 注意：余额为**限时额度**，到期未使用部分将失效（账户余额已包含限时额度）。

---

## 3. 前端 API 端点（登录态）

> 基元律动前端为 SPA，登录后所有数据来自以下 JSON API。认证基于 **session cookie**（详见第 4 节）。

| 端点 | 方法 | 用途 |
|------|------|------|
| `/api/auth/me` | GET | 登录状态检查 |
| `/api/me` | GET | 用户基础信息（id / name / phone / status / role / features） |
| `/api/usage-summary` | GET | **用量 + 余额汇总（核心）** |
| `/api/api-keys` | GET | API Key 列表 |
| `/api/call-logs/page` | GET | **调用日志分页（详见第 4.7 节）** |
| `/api/export-tasks/page` | GET | 异步导出任务列表 |
| `/api/models` | GET | 平台模型列表 |
| `/api/wallet/expiring-credits?page=1&pageSize=20` | GET | 限时额度明细（来源/发放/剩余/到期） |
| `/api/referrals/me` | GET | 邀请信息 |
| `/api/site-content` | GET | 站点内容（无需登录） |

---

## 4. 通过 Cookie 查余额（核心）

### 4.1 认证原理

- 登录后服务器下发 **httpOnly Cookie**，JS 无法读取，浏览器自动附带：
  - **`tr_session`** — 登录会话凭据（`HttpOnly`、`Secure`、`SameSite=Lax`）
  - `tr_ref_device` — 设备/邀请标识（`HttpOnly`）
- 请求带 cookie → `200`；不带 → `401`（已实测验证）
- 前端 API 不走 `Authorization: Bearer`（那是给 `/v1/*` LLM 接口用的），**用户中心数据全靠 cookie**

### 4.2 余额接口

```
GET https://tokenrhythm.studio/api/usage-summary
Cookie: tr_session=<会话值>
```

响应示例（实测脱敏）：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "calls": 691,
    "successCalls": 651,
    "errorCalls": 38,
    "abortedCalls": 2,
    "inputTokens": 96740434,
    "outputTokens": 210554,
    "costCny": 14.49150388,
    "balanceCny": 53.50849612,
    "frozenBalanceCny": 0,
    "availableBalanceCny": 53.50849612,
    "expiringBalanceCny": 53.50849612,
    "nextExpiryAt": "2026-09-03T03:20:12.302Z",
    "currency": "CNY"
  },
  "traceId": "trace_xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
}
```

**字段说明：**

| 字段 | 含义 |
|------|------|
| `balanceCny` | **账户余额（¥）** |
| `availableBalanceCny` | 可用额度 |
| `frozenBalanceCny` | 冻结额度（待结算占用） |
| `expiringBalanceCny` | 限时额度（即将到期部分） |
| `nextExpiryAt` | 最近到期时间（ISO 8601，UTC） |
| `costCny` | 累计成本 |
| `calls` / `successCalls` / `errorCalls` / `abortedCalls` | 调用统计 |
| `inputTokens` / `outputTokens` | 累计输入 / 输出 Token |
| `currency` | 币种（CNY） |

### 4.3 获取 Cookie 值

`tr_session` 是 HttpOnly Cookie，**不能用 JS `document.cookie` 读取**。获取方式：

1. **Chrome / Edge / VS Code 内置浏览器**：打开 DevTools（F12）→ Application（应用）→ Storage → Cookies → `https://tokenrhythm.studio` → 找到 `tr_session` 复制 Value
2. **Firefox**：DevTools → 存储 → Cookie → 同上
3. **浏览器扩展**：Cookie-Editor 等扩展可一键导出

### 4.4 命令行查询（curl）

```bash
curl -s "https://tokenrhythm.studio/api/usage-summary" \
  -H "Cookie: tr_session=你的会话值" \
  -H "Accept: application/json"
```

只取余额：

```bash
curl -s "https://tokenrhythm.studio/api/usage-summary" \
  -H "Cookie: tr_session=你的会话值" | jq '.data.balanceCny'
```

### 4.5 Python 查询脚本

```python
import requests

COOKIE = "tr_session=你的会话值"  # 从浏览器 DevTools 获取

resp = requests.get(
    "https://tokenrhythm.studio/api/usage-summary",
    headers={"Cookie": COOKIE, "Accept": "application/json"},
)
resp.raise_for_status()
data = resp.json()["data"]

print(f"账户余额   : ¥{data['balanceCny']:.2f} {data['currency']}")
print(f"可用额度   : ¥{data['availableBalanceCny']:.2f}")
print(f"冻结额度   : ¥{data['frozenBalanceCny']:.2f}")
print(f"限时额度   : ¥{data['expiringBalanceCny']:.2f}")
print(f"最近到期   : {data['nextExpiryAt']}")
print(f"累计成本   : ¥{data['costCny']:.2f}")
print(f"累计调用   : {data['calls']} 次（成功 {data['successCalls']} / 错误 {data['errorCalls']}）")
```

> 也可以直接用浏览器控制台（已登录状态）验证：
> ```js
> fetch('/api/usage-summary', { credentials: 'include' }).then(r => r.json()).then(d => console.log(d.data.balanceCny))
> ```

### 4.6 多账号批量查询（多 Cookie）

支持一次性传入多个 `tr_session`，分别查询每个账号的余额并汇总。

```python
import requests

# 每个元素是一个账号的 tr_session Cookie 值（从各账号浏览器 DevTools 获取）
COOKIES = [
    "你的账号1的tr_session值",
    "你的账号2的tr_session值",
    # ...
]

def query_balance(cookie: str) -> dict:
    resp = requests.get(
        "https://tokenrhythm.studio/api/usage-summary",
        headers={"Cookie": f"tr_session={cookie}", "Accept": "application/json"},
        timeout=10,
    )
    if resp.status_code == 401:
        return {"error": "401 未认证（Cookie 失效或格式错误）"}
    resp.raise_for_status()
    return resp.json()["data"]

print(f"{'#':<3} {'余额(¥)':<12} {'可用(¥)':<12} {'冻结(¥)':<12} {'限时(¥)':<12} {'最近到期':<24} {'累计调用':<8} {'累计成本(¥)'}")
print("-" * 110)
total = 0.0
for i, cookie in enumerate(COOKIES, 1):
    d = query_balance(cookie)
    if "error" in d:
        print(f"{i:<3} {d['error']}")
        continue
    total += d["balanceCny"]
    print(
        f"{i:<3} {d['balanceCny']:<12.2f} {d['availableBalanceCny']:<12.2f} "
        f"{d['frozenBalanceCny']:<12.2f} {d['expiringBalanceCny']:<12.2f} "
        f"{d['nextExpiryAt']:<24} {d['calls']:<8} {d['costCny']:.2f}"
    )
print("-" * 110)
print(f"合计余额: ¥{total:.2f}")
```

### 4.7 查询每次调用日志

#### 网页入口

用户中心 → **调用日志**（`https://tokenrhythm.studio/account/logs`）

**筛选能力**（页面顶部）：
- 追踪 ID（traceId）精确搜索
- 模型下拉筛选（全部 / DeepSeek V4 Flash / GLM-5 / Kimi K2.6 等）
- 状态筛选（全部 / 成功 / 错误）
- 时间范围（开始/结束时间选择器）
- 快捷时间：最近 1 小时 / 3 小时 / 今天 / 3 天 / 7 天
- **异步导出**按钮：导出调用日志为文件

**表格列**：时间、模型、协议、调用来源、调用方式、状态、耗时、用量、成本、追踪 ID

#### API 接口

```
GET https://tokenrhythm.studio/api/call-logs/page?startAt=<ISO8601_UTC>&endAt=<ISO8601_UTC>&page=1&pageSize=10
Cookie: tr_session=<会话值>
```

- `startAt` / `endAt`：UTC 时间的 ISO 8601 格式，如 `2026-08-03T16:00:00.000Z`
- `page` / `pageSize`：分页（默认 pageSize=10，可调大如 100）
- 响应结构：`{ code, message, data: { list: [...] } }`

#### 单条日志字段（实测）

| 字段 | 含义 |
|------|------|
| `requestAt` / `time` | 请求时间（UTC ISO 8601 / 本地 HH:mm:ss） |
| `endpoint` | 端点（如 `/v1/chat/completions`） |
| `apiSurface` | 协议（`openai` / `anthropic` / `responses`） |
| `requestId` / `traceId` | 追踪 ID（`trace_...`） |
| `model` / `provider` | 模型显示名 / 提供商（如 DeepSeek） |
| `requestModelId` / `actualModelId` | 请求模型 ID / 实际路由模型 ID |
| `keyName` | 使用的 API Key 名称（如"公司电脑copilot"） |
| `clientApp` / `agent` | 客户端应用标识（如 `ai-sdk`） |
| `source` / `sourceLabel` | 调用来源（`api_key` / "API Key 调用"） |
| `stream` | 是否流式 |
| `status` | HTTP 状态码（200 / 错误码） |
| `latencyMs` | 耗时（毫秒） |
| `inputTokens` / `outputTokens` / `totalTokens` | Token 用量 |
| `cacheReadTokens` / `cacheCreationTokens` | 缓存命中 / 写入 Token |
| `reasoningTokens` | 推理 Token |
| `costCny` | 本次成本（¥） |
| `actualCostUsd` | 本次成本（USD） |
| `finishReason` | 结束原因（如 `tool_calls` / `stop`） |
| `retryCount` | 重试次数 |
| `streamInterrupted` / `clientDisconnected` | 流中断 / 客户端断开标记 |

#### Python 批量拉取最近 N 条日志

```python
import requests
from datetime import datetime, timezone, timedelta

COOKIE = "tr_session=你的会话值"

def fetch_call_logs(cookie: str, hours: int = 24, page_size: int = 100) -> list:
    now = datetime.now(timezone.utc)
    start = (now - timedelta(hours=hours)).isoformat().replace("+00:00", ".000Z")
    end = now.isoformat().replace("+00:00", ".000Z")
    resp = requests.get(
        "https://tokenrhythm.studio/api/call-logs/page",
        params={"startAt": start, "endAt": end, "page": 1, "pageSize": page_size},
        headers={"Cookie": f"tr_session={cookie}", "Accept": "application/json"},
        timeout=10,
    )
    resp.raise_for_status()
    return resp.json()["data"]["list"]

logs = fetch_call_logs(COOKIE)
print(f"{'时间':<20} {'模型':<24} {'状态':<6} {'耗时':<10} {'输入':<10} {'输出':<8} {'成本(¥)':<12} {'追踪ID'}")
for log in logs:
    print(
        f"{log['requestAt']:<20} {log['model']:<24} {log['status']:<6} "
        f"{log['latencyMs']}ms   {log['inputTokens']:<10} {log['outputTokens']:<8} "
        f"{log['costCny']:<12.6f} {log['traceId']}"
    )
```

---

## 5. LLM API（与扩展对接）

> 详见 `.copilot/api-reference.md`，此处仅列关键点。

- 统一基础地址：`https://tokenrhythm.studio/v1`
- 认证：`Authorization: Bearer <API_KEY>`
- 端点：
  - `GET /v1/models` — 模型列表（含 `supports_responses` / `supports_anthropic` 能力标记）
  - `POST /v1/chat/completions` — OpenAI 兼容
  - `POST /v1/messages` — Anthropic 兼容
  - `POST /v1/responses` — Responses API
  - `POST /v1/embeddings` — 向量嵌入

> **注意**：`/v1/*` 用 API Key（Bearer），`/api/*` 用户中心用 session cookie（`tr_session`），两者认证体系不同，勿混用。

---

## 6. 注意事项

1. **Cookie 即凭证**：`tr_session` 等同于登录态，泄露后他人可直接查看/操作你的账户，切勿提交到 Git、截图外发或分享。
2. Cookie 有效期由服务端控制，过期后需重新登录获取。批量查询时若某个 Cookie 返回 `401`，说明该账号会话已失效。
3. **多账号批量查询**：每个 Cookie 对应一个账号，脚本按顺序逐个请求（未加并发，避免触发风控）；如账号较多，建议在两次请求间加 `time.sleep(0.5)` 防止限流。
4. 余额为限时赠送额度，**2026-09-03 到期**，注意在到期前使用。
5. 平台备案主体是"北京基元律动科技有限公司"（官网文案为"基元律动"），日常口头说的"基云律动"指同一平台。
6. 用户中心数据接口为前端私有 API，**无官方文档**，字段可能随版本变更，脚本需容错。
